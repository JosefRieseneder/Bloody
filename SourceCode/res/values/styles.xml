<resources xmlns:android="http://schemas.android.com/apk/res/android">

    <!--
        Base application theme, dependent on API level. This theme is replaced
        by AppBaseTheme from res/values-vXX/styles.xml on newer devices.
    -->
    <style name="AppBaseTheme" parent="android:Theme.Light">
        <!--
            Theme customizations available in newer API levels can go in
            res/values-vXX/styles.xml, while customizations related to
            backward-compatibility can go here.
        -->
    </style>

    <!-- Application theme. -->
    <style name="AppTheme" parent="AppBaseTheme">
        <item name="android:buttonStyle">@style/button_style</item>
        <item name="android:textSize">@dimen/text_size_xxxlarge</item>
    </style>

    <!-- Button style. -->
    <style name="button_style" parent="android:style/Widget.Button">
        <item name="android:background">@drawable/button_blue_selector</item>
        <item name="android:textColor">@color/pure_black</item> <!-- How to specify different color for different state? -->
        <!-- <item name="android:textSize">@dimen/text_size_xxxlarge</item> -->
    </style>

</resources>